"""Il composition root: l'unico punto che conosce le classi concrete.

Vuoto per ora — i comandi arrivano al Task 11 del piano. Il file esiste già perché il
posto in cui si costruiscono gli adattatori va deciso prima di averne uno: se lo si
lascia da scrivere, il primo adattatore finisce costruito dove serve, e da lì in poi
`domain` e `application` cominciano a sapere chi li implementa.
"""
